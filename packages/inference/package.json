{
  "name": "@conduit/inference",
  "version": "0.1.0",
  "description": "Conduit inference core: one resolve() every AI request passes through. Runtime-agnostic core plus per-runtime adapters (workers / deno / node) and provider adapters (Anthropic / Workers-AI / OpenRouter). Single source of truth for routing, cost math, and the decision record.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./core": "./src/core.ts",
    "./adapters/node": "./src/adapters/node.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT"
}
