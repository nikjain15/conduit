{
  "name": "@conduit/prompts",
  "version": "0.1.0",
  "description": "Conduit prompt registry: a versioned prompt store and a deterministic resolver that composes named templates and interpolates variables, so the system prompt for a use case is config driven and never hard coded.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@conduit/profile": "0.1.0"
  },
  "license": "MIT"
}
