{
  "name": "@conduit/client",
  "version": "0.1.0",
  "description": "Conduit client: one thin SDK an app imports. The same method surface runs the core in-process (embedded) or calls the conduit-gateway over HTTP (gateway). No external dependencies; fetch and the embedded core are injected by the caller.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT"
}
