{
  "name": "@conduit/guardrails",
  "version": "0.1.0",
  "description": "Conduit guardrails engine: a fail-closed decision function that runs a use case's PII policy, a deterministic prompt-injection detector, structured-output validation, a human-in-the-loop confidence threshold, and mandatory eval floors, then combines the signals into one allow, redact, block, or escalate decision.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@conduit/evals": "0.1.0",
    "@conduit/profile": "0.1.0"
  },
  "license": "MIT"
}
